{
  "poster": "data:image/png;base64,",
  "knowledge": {
    "readme": "## nature-of-code-01-2\n\n### 02",
    "course": "随机游走模型（RandomWalk Mobility）\n\n随机游走模型由首先由爱因斯坦在1926年以数学方式描述。由于自然界中的许多实体会以不可预知的方式移动，因此随机游走模型用来描述这种不稳定的移动。\n\n\n随机游走（random walk）也称随机漫步，随机行走等是指基于过去的表现，无法预测将来的发展步骤和方向。核心概念是指任何无规则行走者所带的守恒量都各自对应着一个扩散运输定律 ，接近于布朗运动，是布朗运动理想的数学状态，现阶段主要应用于互联网链接分析及金融股票市场中。\n"
  },
  "code": "/** -\n * \n *  */\n// let walker;\n\n// function setup() {\n//     frameRate(24);\n//     noLoop();\n//     createCanvas(windowWidth, windowHeight);\n//     walker = new Walker();\n//     background(200);\n//     noStroke();\n//     button = createButton('保存GIF');\n//     button.position(windowWidth - 70, 0);\n//     button.mousePressed(buttonEvent);\n//     async function buttonEvent() {\n//         loop()\n//         let url = await Lab.video.recordCanvas(canvas, 24000);\n//         let im = loadImage(url, () => {\n//             saveGif(im, 'test');\n//         })\n//     }\n//     buttonEvent()\n// }\n\n// function draw() {\n\n//     fill(20);\n//     noStroke();\n//     var r = 30;\n\n//     // Draw the ellipse at the value produced by perlin noise\n//     text('随机游走 Random Walk Traditional', r * 2, height * 0.1 + r * 1.2);\n//     text('by shadow', width - r * 3.5, height - r * 1.5)\n\n//     stroke(random(255))\n//     strokeWeight(0.3)\n//     noFill();\n//     rect(r / 2 + map(noise(random()), 0, 1, 0, r / 2), r / 2 + map(noise(random()), 0, 1, 0, r / 2), width - r * 1.5, height - r * 1.5)\n//     rect(r / 2 + map(noise(random()), 0, 1, 0, r / 2), r / 2 + map(noise(random()), 0, 1, 0, r / 2), width - r * 1.5, height - r * 1.5)\n\n//     walker.step();\n//     walker.render();\n\n// }\n\n// class Walker {\n//     constructor() {\n//         this.x = width / 2;\n//         this.y = height / 2;\n//     }\n\n//     render() {\n//         stroke(random(10, 100));\n//         fill(random(200, 255));\n//         strokeWeight(random(1, 6));\n//         point(this.x, this.y);\n//     }\n\n//     step() {\n//         var choice = floor(random(4));\n//         if (choice === 0) {\n//             this.x += 12;\n//         } else if (choice == 1) {\n//             this.x -= 12;\n//         } else if (choice == 2) {\n//             this.y += 12;\n//         } else {\n//             this.y -= 12;\n//         }\n//         this.x = constrain(this.x, 44, width - 44);\n//         this.y = constrain(this.y, height * 0.2, height - 44);\n//     }\n// }\n\n\n// An array to keep track of how often random numbers are picked\n\nlet randomCounts = [];\nlet total = 20;\n\nfunction setup() {\n  createCanvas(640,360);\n  for (let i = 0; i < total; i++) {\n    randomCounts[i] = 0;\n  }\n}\n\nfunction draw() {\n  background(127);\n  let index = floor(random(total));\n  randomCounts[index]++;\n\n  // Draw a rectangle to graph results\n  stroke(0);\n  strokeWeight(2);\n  fill(255);\n\n  let w = width/randomCounts.length;\n\n  for (let x = 0; x < randomCounts.length; x++) {\n    rect(x*w,height-randomCounts[x],w-1,randomCounts[x]);\n  }\n}",
  "code_length": 16,
  "size": [
    320,
    425
  ],
  "extname": "designlabplugin",
  "version": "0.1.1",
  "package_id": "b2115e53944d487b79fb713372173bdf63969bc9",
  "create_time": 1614820897784
}